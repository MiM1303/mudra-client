import useUserData from "../../../hooks/useUserData";

const MyAccount = () => {
    const [userData] = useUserData();
    return (
        <div>
            
        </div>
    );
};

export default MyAccount;